# Shuddh Swad AI Agent — runtime dependencies
#
# Versions are pinned to a known-good window for Streamlit Community Cloud
# (Python 3.11). Loosen them if you need to, but test first.

# --- Core orchestration ---------------------------------------------------
langgraph>=0.2.0,<0.4.0
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0

# --- LLM SDKs -------------------------------------------------------------
groq>=0.11.0
google-genai>=1.0.0

# --- UI -------------------------------------------------------------------
streamlit>=1.36.0

# --- Misc -----------------------------------------------------------------
python-dotenv>=1.0.0
